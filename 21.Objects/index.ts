let studentId: {
    name: string;
    id: number;
    course: string
} = {
    name: "Ali",
    id: 1203,
    course: "Typescript"
};

console.log(`Student ${studentId.name} with id number ${studentId.id} is learning ${studentId.course} in Governor House.`);